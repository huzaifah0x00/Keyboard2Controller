#include <stdlib.h>
#include <Windows.h>
using namespace std;
int main(){
	while {
	if(GetKeyState('A') & 0x8000/*check if high-order bit is set (1 << 15)*/)
		{
		cout << "bla" << endl;    // do stuff
		}
	}
}
